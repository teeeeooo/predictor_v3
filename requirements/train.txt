# Train/Admin runtime requirements.
# Install example: python3 -m pip install -r requirements/train.txt
#
# Includes Excel dependencies because Train writes XLSX training logs and owns
# the Data Mapping XLSX export follow-up.
-r base.txt
-r ml_runtime.txt
-r excel.txt

PySide6
optuna
