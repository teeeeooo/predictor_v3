# AHRI Analytics Official Calculator Evidence

Raw `input.csv`, `result_m.csv`, `result_m1.csv`, checksums, and provenance files
are immutable evidence captured from the official AHRI Analytics calculator.

AHRI 210/240-2026 applicability and corrected expectations are stored separately
in `2026_expected_overlay.json`; the overlay never rewrites the raw evidence.
