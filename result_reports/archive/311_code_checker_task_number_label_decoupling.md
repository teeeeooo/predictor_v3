# Report 311: Code Checker Task Number Label Decoupling

## Goal

- `tools/code_checker` 내의 렌더링 및 빌드 흐름에 하드코딩되어 있던 `task_number="272"` 잔재를 제거한다.
- `CODEBASE_REFERENCE_MAP.md` 가 특정 task 번호에 종속되지 않고 중립적인 프로젝트 스냅샷/구조 참조 증거로서의 톤을 유지하도록 보정한다.

## Scope

### Included Changes

- `tools/code_checker/renderer.py` 수정: `_render_header` 와 `render_compact_map` 의 `task_number` 인자를 optional(default None)로 변경하고, task number가 없을 시 Generated by... 라인에서 task 괄호 표시를 생략하도록 교정.
- `tools/code_checker/build_reference_map.py` 수정: `build_map` 내 `render_compact_map` 호출 시 하드코딩된 `task_number="272"` 제거.
- `tests/test_code_checker_reference_map.py` 수정: 기존 테스트에서 하드코딩된 task 인자 호출을 제거하고, 렌더링 헤더에 task 번호가 들어가지 않음을 검증하는 단언문 추가.
- `docs/WORK_PLAN.md` 갱신.

### Excluded Scope

- `docs/code_map/CODEBASE_REFERENCE_MAP.md` 직접 수정 및 regeneration 수행 금지.
- ML/Predictor, calculator core 및 UI(ui_tk) 계층 코드 수정 금지.
- `tools/check_code_structure.py` 수정 금지.
- active report lifecycle cleanup 보류.

## Verification

- `python3 -B -m py_compile tools/code_checker/*.py` 컴파일 성공 확인.
- `python3 -B -m pytest tests/test_code_checker*.py -rs -vv` 11개 테스트 모두 통과.
- `python3 -B tools/code_checker/build_reference_map.py --check` 실행 결과 `METADATA_MISSING` 상태로 정상 작동 및 exit code 0 확인.
- `python3 -B tools/check_code_structure.py` warnings 4개(기존 warnings)만 발생하고 정상 통과.
- `git diff --check` 및 `git status --short` 확인 완료.

## Reference Evidence Gate & Wording Policy

- **Task-Number Decoupling**: 코드 체커 생성물이 과거 task에 고착되지 않고 중립적이며 유연하게 활용될 수 있도록 라벨을 완전히 분리함.
- **Wording Policy**: durable docs 및 리포트 본문에는 exact active report count 대신 `active report count exceeds lifecycle threshold; cleanup pending`과 같은 threshold wording만 표기하여 리포트 생성 전후의 count 불일치 방지.

## Active Report Count

- active report count exceeds lifecycle threshold; cleanup pending.

## Lifecycle Maintenance Note

- **Pending**: 이번 작업은 lifecycle cleanup이 아니며, active report 갯수가 임계값(10)을 초과하였으나 cleanup은 pending 상태로 유지하고 추후 별도 follow-up 작업으로 처리함.

## Next Suggested Action

1. **Regenerate reference map and commit milestone changes**
2. **Controller switch arc final summary / closeout**
3. **Active report lifecycle cleanup**
4. **Main table migration check**
5. **ui_tk folder cleanup**
6. **EN14825 / AHRI 210/240 / KS profile expansion**

## Commit / Push

- Commit command: `git commit -m "tools: Decouple reference map from task labels"` (no-verify 사용 금지)
- Pushed successfully.
