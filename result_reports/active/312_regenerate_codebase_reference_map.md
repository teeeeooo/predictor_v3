# Report 312: Regenerate Codebase Reference Map

## Goal

- `tools/code_checker` 도구 고도화 완료 이후 실제로 `CODEBASE_REFERENCE_MAP.md`를 재생성하고 정렬 상태를 동기화한다.
- 생성물 내 git metadata 주석 주입 및 task number label 결합 해제를 최종 검증한다.

## Scope

### Included Changes

- `docs/code_map/CODEBASE_REFERENCE_MAP.md` 재생성 (metadata comment 포함 및 헤더에서 task number 문구 제거).
- `docs/WORK_PLAN.md` 및 `project_log.md` 갱신.

### Excluded Scope

- Python source 코드 및 `tools/code_checker` 로직 수정 금지.
- `tools/check_code_structure.py` 수정 금지.
- 이번 슬라이스에서의 active report lifecycle cleanup 보류.
- 커밋 후 `git HEAD` 가 바뀌면서 발생하는 freshness stale 판정을 fresh로 되돌리기 위한 무한 재생성 루프(재생성-커밋 루프) 차단.

## Verification

- `python3 -B tools/code_checker/build_reference_map.py` 재생성 완료 (230 lines).
- `docs/code_map/CODEBASE_REFERENCE_MAP.md` 내용 검증:
  - 1라인: `<!-- CODE_CHECKER_METADATA: {"generated_at_utc": "...", "generator": "code_checker", "git_commit_short": "eac5973", "git_dirty": false, "schema_version": "1.0.0"} -->` 정상 주입 확인.
  - 4라인: `> Generated by code_checker on 2026-06-09 17:00 UTC.` 로 task number 없이 중립적 렌더링 확인.
- `python3 -B tools/code_checker/build_reference_map.py --check` 실행 결과 `Freshness status: FRESH` 및 dirty status warning 확인 (commit 전 시점 기준).
- `python3 -B -m pytest tests/test_code_checker*.py -rs -vv` 11개 테스트 모두 통과.
- `python3 -B tools/check_code_structure.py` (warnings: 4 pre-existing, 0 new) 통과.
- `git diff --check` 및 `git status --short` 확인 완료.

## Reference Evidence Gate & Wording Policy

- **Commit Freshness Note**: 커밋 후 git HEAD의 short hash가 바뀌기 때문에, 커밋 이후 `--check`를 실행하면 stale로 판정되는 것이 정상적임. 이를 위해 추가 커밋 루프를 만들지 않고 milestone snapshot 맵으로 유지함.
- **Wording Policy**: durable docs 및 리포트 본문에는 exact active report count 대신 `active report count exceeds lifecycle threshold; cleanup pending`과 같은 threshold wording만 표기하여 리포트 생성 전후의 count 불일치 방지.

## Active Report Count

- active report count exceeds lifecycle threshold; cleanup pending.

## Lifecycle Maintenance Note

- **Pending**: 이번 작업은 lifecycle cleanup이 아니며, active report 갯수가 임계값(10)을 초과하였으나 cleanup은 pending 상태로 유지하고 추후 별도 follow-up 작업으로 처리함.

## Next Suggested Action

1. **Controller switch arc final summary / closeout**
2. **Active report lifecycle cleanup**
3. **Main table migration check**
4. **ui_tk folder cleanup**
5. **EN14825 / AHRI 210/240 / KS profile expansion**

## Commit / Push

- Commit command: `git commit -m "docs: Regenerate codebase reference map"` (no-verify 사용 금지)
- Pushed successfully.
