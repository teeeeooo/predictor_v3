# Slice 2D+2E Native macOS Visual Evidence Manifest

- Native rendering target commit: `e0156a35982ba40a0356c371f2d690b16f622107`
- Platform target: native macOS onscreen PySide6 TrainShell; no offscreen Qt
  platform was used for the attempted visual check
- Display wake: attempted through Computer Use; the Mac was locked and the
  tool could not unlock it automatically, so no native window was launched
- Computer Use scope: no screenshot inspection, click, typing, table
  interaction, export interaction, or import interaction was performed
- Native PNG evidence: none added because the desktop remained locked
- Synthetic temporary source: `/tmp/predictor_v3_slice2de_native/runtime_mapping.json`
  copied from `tests/fixtures/mapping/mapping_runtime_equivalent.json`
- Synthetic temporary bundle: `/tmp/predictor_v3_slice2de_native/renamed_external_bundle.csv`
  plus the seven canonical group CSVs, generated by the production exchange
  exporter and successfully parsed by the production import preview service
- Programmatically prepared preview: yes; parser/controller preview was
  prepared from the synthetic bundle and reported `mapping_bundle_v1` with
  seven affected groups, but the production dialog was not rendered because
  the native desktop was locked
- Actual export/import interaction: not performed
- Automated behavior evidence: separate from native visual evidence; Slice 2E
  focused/import/smoke gate passed 77 tests, impacted regression passed 189
  tests, and the full suite passed 2051 tests with 2 expected failures
- Protected data: `data/mapping.json` and the protected runtime fixture were
  unchanged
- Slice 2B+2C physical interaction acceptance remains deferred; this Batch did
  not retry the known Computer Use Qt table accessibility crash path

## Required visual states not captured

1. Data Mapping toolbar with Exchange Package Export and Import visibly
   distinguished from Review Snapshot and clipboard Paste.
2. Programmatically prepared production import preview showing format/version,
   group/add/remove/change counts, blocker/warning regions, and separate Apply
   to Draft / Cancel actions without raw CSV as the primary surface.

The native environment blocker does not invalidate the automated Batch result;
the visual states remain explicitly pending for a later unlocked-desktop audit.
